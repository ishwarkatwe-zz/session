import { AdvDirective } from './adv.directive';

describe('AdvDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvDirective();
    expect(directive).toBeTruthy();
  });
});
